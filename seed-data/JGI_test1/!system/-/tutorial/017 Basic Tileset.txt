In this tutorial you will be making a lot of tiles to use in your game.||(nextButton)|#First open tile maker. Then click new, 32 by 32. These should ALL be 32.||(nextButton)|#Make a portal.||(nextButton)|#Save.||(nextButton)|#Make a health item.||(nextButton)|#Save.||(nextButton)|#Make a point item. ||(nextButton)|#Save.||(nextButton)|#Make a slider.||(nextButton)|#Save.||(nextButton)|#Complete!||(nextButton)|