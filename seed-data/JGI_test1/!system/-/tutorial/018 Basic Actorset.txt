This will help you make tiles into working actors.||(nextButton)|#First open actor maker.||(nextButton)|#Push new, and then floor.||(nextButton)|#Insert the portal tile.||(nextButton)|#Go to item behavior, and change inactive to blocks NPC.||(nextButton)|#Save.||(nextButton)|#Push new, and then item.||(nextButton)|#Put in the healing item tile.||(nextButton)|#Go to item behavior. Change inactive to player picks up, uses immediately.||(nextButton)|#You can change the healing amount to whatever you want.||(nextButton)|#Save.||(nextButton)|#Push new, then item.||(nextButton)|#Put in the point tile.||(nextButton)|#Go to item behavior. Have it a reasonable amount of points to recieve from it.||(nextButton)|#Save.||(nextButton)|#Push new, and then slider.||(nextButton)|#Insert the slider tile.||(nextButton)|#Go to item behavior. Change "player can push block to slide it" to 1 or the max.||(nextButton)|#Save.||(nextButton)|#Complete!||(nextButton)|