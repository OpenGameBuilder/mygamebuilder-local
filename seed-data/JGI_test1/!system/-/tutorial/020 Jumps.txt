Jumps are like portals. They transfer your player from one map to another.||(nextButton)|#They are very easy to use. ||(nextButton)|#Go to map maker.||(nextButton)|#Insert your player.||(nextButton)|#There is a new button near all your tools called jump. Click it||(nextButton)|#Now put it on the map. You will need another map to jump to.||(nextButton)|#Play game. When you touch that point, you will jump. The jump point may be hard to find.||(nextButton)|#This allows you to have multiple levels.||(nextButton)|