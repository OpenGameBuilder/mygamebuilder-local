Let's take a look at the <b>HOME</b> screen in detail - choose it now using the screen bar at the top of the page||(nextButton)|#There are a few areas to explore. Firstly, at the top left is a box that describes your current session||(nextButton)|#In that box, is a tile marked '<b>Avatar</b>'. This is the tile used to represent you in MGB forums and comments (coming soon...)||(nextButton)|#It's easy to create an avatar. Just create and save a tile called <b>avatar</b>||(nextButton)|#If you already have an avatar, a quick short-cut for editing it is to double-click on the avatar shown in this <b>HOME</b> screen||(nextButton)|#Also in this 'Session' box is an area where you can describe your status - just enter anything you like||(nextButton)|#This <b>Session</b> box also has ways for you to change your password or to logout||(nextButton)|#The next box to the right is the <b>Project</b> box. Your user account can have multiple projects, typically each project will be a game||(nextButton)|#If you click on the <b>Change</b> button in the <b>Project</b> box you can choose a new project, or select an existing one||(nextButton)|#When you change projects, all saves & load are now for that project only||(nextButton)|#If you can't find something you saved, it's usually in another project...||(nextButton)|#The <b>avatar</b> tile is special - MGB always expects it to be in a project called <b>project1</b>||(nextButton)|#You can also have a logo tile for each project - like a per-project avatar. Just create a tile called <b>project.logo</b> and it will be shown in the <b>Project box</b>||(nextButton)|#Moving to the right again, the third box is the <b>Skills & Choices</b> box||(nextButton)|#One cool thing you can do here is to <b>vote</b> for features you want to see appear in My Game Builder. ||(nextButton)|#This will also give you a good idea of things you can expect to see soon. Do vote - we do look at the votes carefully to see what our users want||(nextButton)|#You can also use this box to run any tutorials you'd like - either skip dull ones, or go back to previous ones again||(nextButton)|#Finally, there's the <b>My Tiles / My Actors / My Maps<b> box in the middle of the screen - the wide area||(nextButton)|#This <b>My (thing)</b> is used to quickly look through tiles/actors/maps, and you can alsodouble-click an image of a tile/actor/map to load it!||(nextButton)|#<b>Finally</b>, you can also delete things from here - just drag them up to the <b>Trashcan</b> to get rid of them... forever, so be careful||(nextButton)|#Oh one thing.. you can't rename tiles/actors/maps yet. Just thought I'd let you know so you don't go crazy looking :)||(nextButton)|