This tutorial was made by Eradicator117.||(nextButton)|#Remember that this is an advanced tutorial, and assumes that you know the basics of MGB.||(nextButton)|#Homing weapons are "shots" that home in on the player (What else?) and do damage.||(nextButton)|#For now, only enemies can use homing shots.   ||(nextButton)|#First, make a graphic for your homing shooter in Tile Maker.||(nextButton)|#Next make a graphics for your homing shot in Tile Maker.||(nextButton)|#Finished? Make a new enemy, and choose the shot tile. Give it animation if you want.||(nextButton)|#Give it 20 health.||(nextButton)|#Keep everything the same on the Character Behavior page, except change the touch damage against player to whatever you want, and the NPC touch damage to 1.||(nextButton)|#On the NPC behavior page, change the movement to "Move towards player" and allow it to occupy player space.||(nextButton)|#Now save the actor.||(nextButton)|#Now for the shooter. Go to actor maker, make an enemy, and put in the shooter tile.||(nextButton)|#Set it to "No Automatic Movement" in NPC behavior.||(nextButton)|#In destruction/spawning, click the box next to "Generates new actor" and enter your Homing Shot.||(nextButton)|#You are done! Test it out and make adjustments if you want to. Another way to make the shot die is to set the player to do a very low touch damage when an enemy is in its area, so that it takes the damage it is meant to and kill the temy attacking.||(nextButton)|