This topic lists glitches and things that have not been implemented into MGB yet, but may appear that they are.||(nextButton)|#Non-Implemented:||(nextButton)|#NPC melee||(nextButton)|#Shot accuaracy||(nextButton)|#Player and NPC speed||(nextButton)|#Moving in set directions only||(nextButton)|#Glitches:||(nextButton)|#NPC's that have a movement of 0 do not shoot. Just set the NPC behavior to "No automatic movement" while we fix that.||(nextButton)|#Sliders are glitchy in many ways. ||(nextButton)|#Sometimes when you load a map in map maker it is grey, just zoom, turn on and off the grid, or scroll.||(nextButton)|#Accounts are randomly deleted.||(nextButton)|#JOKING!||(nextButton)|#You should be good now, we hope to have all of these done/fixed very soon.||(nextButton)|